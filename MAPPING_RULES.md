# ASPRO 재고 매핑·규칙 명세 (AI 인수인계용)

> 기준일: 2026-09-14 · 기준 코드: `aspro_v42.html`, `ecount_sync/*.mjs`, 운영 Google Sheets
> 목적: 다른 AI/개발자가 이 문서만 읽고 "사방넷 옵션 → 재고 수량"이 어떻게 결정되는지 재현할 수 있게 한다.
> 코드와 문서가 다르면 **코드가 정답**이다. 수정 시 이 문서도 갱신한다.

---

## 0. 한 줄 요약

사방넷 단품(옵션)마다 **물류품번(=재고 원본의 품목코드)** 을 찾아 재고를 가져오고,
운영 예외(강제품절·강제재고·품절처리·발주서품절)를 덮어쓴 뒤,
사방넷 가상재고 파일/하나허브/시트로 내보낸다.

```
사방넷단품원본(옵션 1행) ──► 물류품번 결정 ──► 재고 원본 조회 ──► 운영 예외 적용 ──► 결과
                               (매핑 규칙)       (이카운트/김포/KCFI)   (우선순위 표)
```

---

## 1. 용어

| 용어 | 뜻 | 예 |
|---|---|---|
| 사방넷코드 | 사방넷 단품(옵션) 고유코드. `상품번호-옵션순번` | `102675-0002` |
| 자체코드 | 사방넷 상품의 자체상품코드. 색상/세트 접미사가 붙음 | `26BLCD_GR`, `JW25SS01_AL`, `N1008_SET` |
| 옵션 | 사방넷 옵션 문자열. `색상:사이즈` 가 기본 | `플럼:55`, `블랙+플럼:66`, `55` |
| 물류품번 / 품목코드 | 재고 원본(이카운트·김포)의 SKU 코드 | `252JW07-PL-55`, `NT261GW01-OL-55` |
| prefix | 물류품번의 첫 `-` 앞부분(상품군) | `252JW07` |
| 색상코드 | 물류품번 안의 2~3자 영문 색상 | `BK`, `IV`, `PL`, `WH` |

**물류품번 형식은 섞여 있다** (파서가 둘 다 처리):
- 신패턴 `prefix-색상-사이즈` : `252JW07-PL-55`
- 구패턴 `prefix-사이즈-색상` : `243NT05-55-BK` → 김포에 없으면 자동으로 `243NT05-BK-55` 재시도
- 순번형 `prefix-001` : `23FWNDTS-006`, `252MX18CD-001` (색상/사이즈는 원본 품목명 `[색상/사이즈]`에서 읽음)
- 사이즈 문자 : `NT262GW07-BK-S/M/L`, `...-FREE`, `...-5`

---

## 2. 데이터 원본 (Google Sheets)

운영 시트 ID: `1cl-uZ9s0_VNF_YwagnujAhPTdREuzfcaPisWRRp7zwY`

### 2-1. 재고 원본 탭

| 탭 | 내용 | 파싱 규칙 |
|---|---|---|
| `사방넷단품원본` | 사방넷 단품 전체 | 4행부터 데이터. A=사방넷코드, C=상품명, D=옵션, F=가상재고, T(19)=자체코드 |
| `이카운트원본` | 이카운트 품목·재고 | 3행부터. A=품목코드, B=품번(사방넷 연결용), C=브랜드, E=품목명`[색상/사이즈]`, **F=재고**. A1 셀 = `최종 동기화: …` 시각 |
| `김포원본` (구) / 재고관리시트 `김포재고장` (현) | 김포 물류 재고 | 아래 2-2 |
| `kcfi_원본` / 재고관리시트 `KCFI` | 신원(KCFI) 재고, 상품 블록 구조 | 아래 2-3 |

현재 김포·KCFI는 별도 재고관리 시트 `1iwZL8sVWvXG4G6lJQXnmHcx8DnHp60opjYxlequk5Us`
(김포재고장 gid `462252528`, KCFI gid `492795649`)에서 **export CSV**로 읽는다
(GViz CSV는 숫자/문자 섞인 열의 문자를 빈칸으로 바꾸므로 사용 금지).

### 2-2. 김포 원본 파서

- 상단 10행 안에서 헤더행 자동 탐지: `품목코드` 또는 판매상태 헤더가 있는 행.
- 판매상태 헤더: 공백 제거 후 `판매상태` / `판매여부` / `판매상태값` (A열에 추가됨).
- **비활성 행 제외** (공백 제거·대문자화 후 일치): `판매중지 판매종료 종료 중지 N STOP 정지 비활성 INACTIVE 판매불가 단종`.
- 물류품번 = 행 앞 8칸 중 정규식 `^[A-Z0-9]+-[A-Z0-9]{1,4}-[A-Z0-9]{1,4}(-.+)?$` 첫 셀 (숫자 관리코드 무시).
- 수량 = 품번 칸 이후 **가장 오른쪽 정수 셀**. 같은 품번 여러 행이면 합산.
- `…-CD` 와 `…-SK` 가 둘 다 있으면 `…-SET = min(CD, SK)` 자동 생성.

### 2-3. KCFI 원본 파서 (블록 구조)

```
상품명 행            ← 블록 시작 (A열 값, "이미지"/"TTL" 아님)
이미지|색상|사이즈|실수량(or 전산수량/수량)   ← 헤더 (1~3행 아래)
옵션행…              ← A열 비어도 위 상품명에 귀속
TTL                  ← 블록 끝
```
- 헤더에 `이미지`+`색상`만 있고 사이즈/수량이 빈칸이면 `색상=B, 사이즈=색상+1, 수량=색상+2` 추론.
- 수량 `-` 또는 빈칸 = 0.
- 색상 정규화: 괄호 제거 `블랙(공통)→블랙`, 공백 제거.
- 색상 별칭: `블루↔틸블루/스카이블루/라이트블루`, `그레이↔라이트그레이/차콜`, `화이트↔아이보리/크림`.
- 상품명 → 사방넷 prefix 결정 순서: ① 대시보드에서 사용자가 누른 `사방넷 매핑`(직접맵) ② 코드 상수 `KCFI_PRODUCT_MAP` ③ 상품명 키워드 점수 자동추정(최고점 ≥2, 동점 없음일 때만).
- 결과는 `aspro_kcfi`(사방넷코드→재고)에도 백업.

### 2-4. 매핑·예외 탭 (사람이 관리하는 규칙 데이터)

| 탭 | 키 | 값 | 의미 | 현재 행수 |
|---|---|---|---|---|
| `aspro_match` | 사방넷코드 | 김포물류품번(A:사방넷코드, B:물류품번, C:상품명, D:메모, E:등록일) | **옵션별 물류품번 매핑 마스터** | ~2,990 |
| `aspro_iccodemap` | 사방넷코드(또는 자체코드) | 이카운트 품목코드 prefix | 이카운트 직접 지정 | 소수 |
| `aspro_setmap` | 자체코드 prefix | 물류품번 prefix + 색상매핑(`한글:코드,…`) | 색상→코드 규칙 / 세트 구성 | 4 |
| `aspro_soldout` | 사방넷코드 | 등록일 | 매핑 없는 옵션을 **품절(0)** 처리 | ~209 |
| `aspro_forcezero` | 자체코드/prefix/사방넷코드 | 사유 | **강제품절** (재고 무시 0) | ~22 |
| `aspro_forcestock` | 사방넷코드 | 강제재고 수량 | **강제재고** (리오더 신상품, 재고 무시 고정값) | ~20 |
| `aspro_soldout_logistics` | 물류품번(색상·사이즈 포함 전체코드) | 색상/사이즈/상품명/출처/메모 | **발주서품절** (해당 SKU만 0) | ~150 |
| `aspro_normname` | 정규화상품명 | 물류품번 | 이름 기반 자동매핑 기록 | — |

`aspro_setmap` 현재 내용:
```
MS25S      | 252MX05    | 플럼:BU,차콜:GR,그레이:GR,베이지:BE,블랙:BK
JW25SS02   | 252JW19    | 아이보리:IV,스카이블루:BL,블랙:BK
252JW10    | 252JW10    | 화이트:IV,블랙:BK,블루:BL,아이보리:IV
N1008_SET  | 24FWELTS01 | 블랙:BK,브라운:BR,베이지:BE
```
코드 기본값(시트 로드 전 폴백, 시트가 있으면 **병합**되어 기본값이 사라지지 않음):
`JW25SS01 → 252JW07 {블랙:NA, 네이비:NA, 아이보리:IV, 플럼:PL, 플림:PL}` 등.

---

## 3. 최종 재고 결정 우선순위 (`runMatch`)

사방넷 옵션 1행마다 **위에서부터 처음 걸리는 것**을 사용한다.

| 순위 | 조건 | 결과 `src` | 수량 |
|---|---|---|---|
| 1 | 자체코드 / 자체코드 prefix / 사방넷코드 ∈ `aspro_forcezero` | `강제품절` | 0 |
| 2 | 사방넷코드 ∈ `aspro_forcestock` | `강제재고` | 지정값 |
| 3 | KCFI 재고 존재 & > 0 | `KCFI` | KCFI 수량 |
| 4 | 김포에서 찾음 (재고 0 포함) | `김포`, `김포(규칙)`, `김포(세트)`, `김포(÷N)`, `김포(복사상품)` | 김포 수량 |
| 5 | 이카운트에서 찾음 | `이카운트`, `이카운트(세트)`, `이카운트(합산)`, `이카운트(자동품절)`, `이카운트(세트품절)`, `이카운트(컬러불일치)`, `이카운트(저장매핑)` | 이카운트 수량 |
| 6 | KCFI 재고 존재 & = 0 | `KCFI` | 0 |
| 7 | 아무것도 없고 사방넷코드 ∈ `aspro_soldout` | `품절처리` | 0 |
| 8 | 아무것도 없음 | (매핑없음 탭으로) | null → `unmatched` |
| 후처리 | 위 결과 수량>0 이고 `src≠강제재고` 이고 **최종 물류품번** ∈ `aspro_soldout_logistics` | `발주서품절` | 0 |

핵심 원칙
- **김포와 이카운트가 둘 다 잡히면 김포 우선** (실제 출고 기준이 김포. 이카운트 우선 시 오출고 사고).
- 강제재고는 리오더 상품이라 발주서품절에 걸려도 유지.
- 상태: `null→unmatched`, `0→zero`, `>0→matched`.

> ⚠ 운영 자동 재계산(`daily-recalculate.mjs`)은 2026-09-11부터 **이카운트 단독 모드**(`?stockSource=ecount`)가 기본이라
> 3·4·6(KCFI/김포)은 건너뛴다. 김포·KCFI 혼합은 `--combined-sources`로만 켠다. 섹션 7 참고.

---

## 4. 김포 조회 규칙 (`getGimpoQty`)

순서대로 시도한다.

### 4-1. 자체코드 규칙 (단품, 옵션에 `+` 없음)
1. 자체코드에서 규칙키 후보 생성: 원본 → `_AL`/`_AL_n` 제거 → `_SET`/`-SET`/`_nSET` 제거 → `-` 앞 → `_` 앞.
2. `setRuleMap`(=코드기본값 ⊕ `aspro_setmap`)에서 첫 일치 규칙 사용.
3. 옵션 `색상:사이즈` 파싱(순서 반대 `55:블랙`도 인식).
4. 색상코드 = 규칙 `colorMap` → 전역 `COLOR_MAP` → 원문.
5. 조회키 `gpPrefix-색상코드-사이즈`. 김포에 없으면 **재고 0 (`김포(규칙)`)** — 매핑없음으로 떨어뜨리지 않는다.

예: `JW25SS01_AL` / `플림:55` → 규칙 `JW25SS01` → `플림→PL` → `252JW07-PL-55`.

### 4-2. 세트 옵션 (옵션에 `+` 와 `:`)
- 예 `블랙+플럼:66` → 규칙으로 각 색상 코드화 → 구성 SKU 목록.
- 같은 색상이 N번이면 그 SKU 재고 ÷ N (내림). 예 `플럼+플럼:55` = PL-55 재고 / 2.
- 세트 재고 = 구성 SKU별 (재고÷필요수) 의 **최솟값**. 구성 SKU 하나라도 없으면 null(매핑없음).
- `src=김포(세트)`, 물류품번 = `A+B` 형태.

### 4-3. 저장 매핑
1. 물류품번 = `EXTRA_STATIC_MAP`(코드 하드코딩) → `extraMap`(대시보드 수정분) → `aspro_match`. `__DELETED__`면 매핑 없음.
2. 없으면 `정규화상품명||정규화옵션` 으로 이름매핑(normOptMap) 시도.
3. `resolveLogisticsCode`: **품번 변경 자동변환** `252MX18-001~016 → 252MX18CD-001~016`.
4. 김포에 없으면 구패턴→신패턴(`prefix-55-BK → prefix-BK-55`) 재시도.
5. 김포에 없으면 이카운트에서 같은 품번군의 품목명 `[색상/사이즈]`로 옵션 매칭 시도 → 성공 시 `이카운트`.
6. 둘 다 없으면 `김포` 재고 0.
7. 김포에 있으면: **같은 물류품번·같은 사이즈에 매핑된 사방넷 옵션이 N개**면 재고 ÷ N (`김포(÷N)`). 저장 매핑 없이 이름으로 찾았으면 `김포(복사상품)`.

---

## 5. 이카운트 조회 규칙 (`getIcountQty` → `resolveIcItems`)

이카운트 원본은 `prefix`(품목코드 `-` 앞)와 `품번(B열)` 두 키로 인덱싱. 색상/사이즈는 **품목명 안의 `[색상/사이즈]`** 에서 읽는다 (`-공통` 접미사 제거).

| 순위 | 방법 |
|---|---|
| 0 | `DIRECT_QTY_MAP` 고정값 (예: `102579-0001 → N1009-009 = 26`) |
| 1 | 직접매핑: `aspro_iccodemap`[사방넷코드/자체코드/각 prefix] → `IC_STATIC_MAP`[자체코드] → (세트면) setmap의 gpPrefix |
| 2 | 자체코드 / `SAB_IC_OVERRIDE` / 자체코드 `-`앞 / 끝 색상코드 제거 prefix 로 조회 |
| 3 | 이름매칭: 정규화 상품명 완전일치 → 키워드 교집합(2개 이상 또는 4글자 이상 단어 1개) |

색상코드 결정: 옵션 색상 → `COLOR_MAP`; 옵션에 색상이 없으면 자체코드 끝 2~3자 대문자(`…GR`→그린). 자체코드별 색상 치환 `SAB_COLOR_OVERRIDE` (예 `N1009_AL: 썸머브라운→BW, 리이트그레이(오타)→LG`).

사이즈만 있는 `_SET` 옵션(예 `N1008_SET / 55`): `aspro_setmap` colorMap 값 전체를 구성 색상으로 보고 세트 처리.

결과 판정:
- 세트: 구성 SKU 모두 있으면 `min(재고)` → `이카운트(세트)`. 하나라도 없으면 직접매핑이면 `이카운트(세트품절)`=0, 이름매칭이면 null.
- 단품 색상+사이즈 일치 → `이카운트`.
- 불일치: 직접매핑이면 같은 사이즈 없음 → `이카운트(자동품절)`=0 / 사이즈는 있는데 색상 없음 → `이카운트(컬러불일치)`=0. 이름매칭이면 null(오매칭 방지).
- 사이즈만 있고 색상 없음: 1건이면 그 수량, 여러 건이면 합산 `이카운트(합산)`.
- 이카운트 단독 모드에서는 저장된 물류품번이 이카운트 코드에 그대로 있으면 우선 `이카운트(저장매핑)`.

`IC_STATIC_MAP` 대표값: `JW25SS01*→252JW07`, `JW25SS02*→252JW19`, `H1014*→H1014`, `251JW_AL→251JW`, `252JW10*→252JW10`, `N1009_AL/N1016_AL`, `NT244G0069-73JK→244NT69` 등 (코드 참고).

---

## 6. 색상 코드 사전 (`COLOR_MAP`, 공통)

| 한글/영문 | 코드 | | 한글/영문 | 코드 |
|---|---|---|---|---|
| 블랙 BLACK | BK | | 베이지 BEIGE BG | BE |
| 네이비 NAVY NV | NA | | 카키 KHAKI | KH |
| 화이트 WHITE | WH | | 브라운 BROWN | BR |
| 아이보리 IVORY | IV | | 크림 CREAM | CR |
| 그레이 GRAY GY | GR | | 오트밀 OATMEAL | OT |
| 차콜 CC | CH | | 퍼플 PURPLE | PU |
| 다크/딥그레이 | DG | | 플럼 PL | PL |
| 라이트그레이 | LG | | 와인 WINE WI | WN |
| 블루 BLUE | BL | | 올리브 | OL |
| 레드 RED | RD | | 민트 | MT |
| 그린/딥그린 GREEN | GN | | 옐로우 YL | YE |
| 핑크 PINK | PK | | 라벤더 LA | LV |
| 모카 | MO | | 오렌지 | OR |

**주의: 사방넷 색상명 ≠ 물류 색상코드인 상품이 많다.** 전역 사전으로 풀리지 않으면 반드시 상품별 규칙(`aspro_setmap` 또는 `SAB_COLOR_OVERRIDE`)으로 명시한다. 확인된 예:

| 자체코드 | 사방넷 표기 | 실제 물류 |
|---|---|---|
| `JW25SS01*` (252JW07) | 블랙 / 플림(오타) | NA(네이비) / PL |
| `MS25S*` (252MX05) | 플럼 / 차콜 | BU / GR |
| `252JW10`, `252NT41` | 화이트 | IV (김포에 WH 없음) |
| `26BLCD_GR / _NA / _PK` (NT261GW01) | 애플그린 / 네이비 / 인디핑크 | OL / BK / PK |
| `LN-KT-OV` (NT262GW02) | 올리브 | GN |
| `EN-OP-WT`, `KN-PT-WT` | 화이트(WT) | WH |
| `AMWNT2504*` | 연핑크 | 핑크 |
| `MX243G0001-6OPJK-SET` (243MX01) | 블루 | 틸블루, 원피스+자켓 2구성 |

---

## 7. 운영 파이프라인 (2026-09-14 현재)

### 7-1. 흐름
```
① 이카운트 웹 로그인 → 재고현황 창고별(횡) → 0품목 포함 → Excel(ESZ018R.xlsx) 수동 다운로드
② node import-warehouse-xlsx.mjs --file=… [--confirm]
     본창고 01 + 온라인창고 02 만 합산 (쿠팡 06, 모모 09 등 제외)
     → logs/last_full_inventory.json  (stockBasis=hana-ecount-01-02-v1)
③ node daily-recalculate.mjs --force [--confirm]
     헤드리스 Chrome으로 aspro_v42.html?stockSource=ecount 실행 → runMatch()
     → restoreEcountMappings() → verifiedEcountPayload()
④ --confirm 시: publish-sheets.mjs(이카운트원본 F열·SKU리스트 H/I) → 하나허브 Firestore → 허브 SKU 동기화
⑤ logs/sabangnet-stock-queue.json 생성 (사방넷 전송 대기. 실제 업로드 미연결)
```

### 7-2. 창고 정책 (`warehouse-policy.mjs`)
- 포함 창고 `['01','02']` 고정. 두 창고 중 하나라도 원본에 없으면 거부.
- 품목·창고 중복, 비정수 수량, 합산 불일치, 3일 초과 원본 거부.
- 음수 재고는 창고 합산 **후** 판매단에서 0으로 제한(먼저 0으로 바꾸면 합계가 부풀려짐).
- 전체합계 API 캐시에 표지만 붙여 재사용 금지.

### 7-3. 매핑 복원 (`ecount-mapping.mjs` / `restoreEcountMappings`) — 이카운트 단독 모드의 최종 판정
**원칙: 저장된 상품 정체성만 재사용한다. 이름 유사도·같은 사이즈만으로 연결하지 않는다.**

1. `강제품절 / 강제재고 / 품절처리 / 발주서품절` 결과는 자동 판매 제외 → 검수(`운영예외 검토`).
2. prefix 결정 순서: iccodemap → 저장 물류품번 prefix → `IC_STATIC_MAP` → `SAB_IC_OVERRIDE` → setRule gpPrefix → 자체코드 → 자체코드에서 `_AL/_SET/_nSET/_n` 제거 → `-` 앞. 이카운트에 없으면 `prefixAliases`로 치환:
   ```
   253NT20→253NT20SK   244NT46CD→244NT46   251NT10CD→251NT10CT
   253NT351→253NT35    253NT25→253NT25PT   253NT19L→253NT19PT
   253NT24→253NT24OP   244NT50AC→244NT50
   ```
   그래도 없으면: 자체코드가 이카운트 prefix + (`AL|SET|색상2~3자`) 형태로 **유일**하게 떨어질 때만, 또는 같은 상품명(대괄호·공백 제거 8자 이상)이 이미 **단 하나의** prefix에 매핑돼 있을 때만 사용.
3. 옵션 파싱(`parseOption`): `색상:사이즈`, `사이즈:색상`, `55(블랙)`, `(라운드)/(카라)` 접두 제거, `+` 세트. 사이즈 정규화: 대문자, `(n)` 제거, `-공통` 제거, `FREE/프리→F`.
4. 색상 결정: 옵션 → (없으면 상품명 끝 `[색상]`) → 상품별 치환 → `COLOR_MAP` 연쇄 치환(최대 3회) → 이카운트에 그 색상이 없으면 setRule colorMap → 그래도 없으면 **한글 포함관계가 유일한** 원본 색상만.
5. 사이즈/색상이 옵션에 없으면 저장 물류품번의 토큰에서 **유일할 때만** 보완.
6. 저장 물류품번이 이카운트에 존재하고 색상·사이즈가 정확히 일치하면 그대로 `이카운트(저장매핑)`.
7. 그 외 prefix 안에서 `색상+사이즈` 일치가 **정확히 1건**일 때만 연결. 0건/복수건이면 검수.
8. 세트 수량 `setStock`: 같은 단품이 N개 필요하면 `floor(max(0,재고)/N)` 의 최솟값.
9. 실패 사유 코드(`reviewReason`): `상품 연결 근거 없음`, `기존 매핑 품번의 현재 원본 확인`, `사이즈 확인 필요`, `색상/세트 구성 확인 필요`, `동일 색상·사이즈 원본 복수`, `색상·사이즈 원본 불일치`, `구성품 수량 확인`, `원피스·자켓 구성 확인`, `옵션 형식 확인`.

### 7-4. 최종 검증 (`verified-ecount.mjs`)
- 이카운트 원본 1,500코드 미만이면 중단.
- 옵션이 판매 재고로 인정되려면: `src`가 `이카운트…`, 물류품번의 모든 코드가 원본에 있고, 재계산한 `setStock`과 수량이 **정확히 같고**, 자동품절/컬러불일치/세트품절이 아니어야 한다.
- 아니면 `unmatched`(재고 0 확정이 아님) → **사방넷 전송 제외**.

### 7-5. 반영 안전장치 (`daily-recalculate.mjs`)
- 사방넷 옵션 < 3,000 / 이카운트 코드 < 1,500 이면 중단.
- 평일은 당일 원본만, 주말은 3일 이내만 허용.
- 상품 < 400 또는 옵션 < 3,000 결과면 중단.
- (혼합 모드) 미연결 > 25% 면 중단, 김포 < 500 / KCFI < 50 / 수동 원본 3~5일 초과 시 중단.
- 같은 날 이미 `confirmed` 성공이면 `--force` 없이는 건너뜀.
- `--audit-only`: 원격 쓰기 차단(GET만 허용), `logs/mapping-audit.json`만 생성.

---

## 8. 사방넷 내보내기 규칙 (`doExport`, 대시보드 수동)

- 대상: 전체 또는 매칭된 행만.
- 내보내는 수량 = `matched`면 재고, 그 외 0.
- 안전재고 옵션: `floor(재고 × N%)` (기본 70%) → 결과가 `0 < 값 < M` 이면 0 (기본 M=5).
- F열(가상재고) = 수량, G열(공급상태) = 수량>0 ? `1`(판매) : `2`(품절).

---

## 9. 매핑 저장·검증 규칙

- 유효하지 않은 물류품번은 저장/로드 시 버림: 빈값, `0`, 5자리 숫자, `#N/A`, `품절`, `확인요망`, `슈컴퍼니…`, `김포재고O…`, `삭제요청…`, `KCFI 코드없음`, `공통색상코드없음`, `???`/`중복`/`위에` 포함.
- 사방넷 단품에 없는 사방넷코드 매핑은 정리하되 `_SET`/`-SET` 코드는 보호.
- 저장 시 `resolveLogisticsCode`로 품번 변경 정규화.
- 대시보드 수정분은 localStorage(`aspro_sab_gimpo_map_v2`)와 Sheets 양쪽. 품절관리도 Sheets(`aspro_soldout`)와 병합 복원.
- `택1` 상품 자동매핑: 매핑없음 중 상품명에 `택1`이 있으면 이미 매핑된 같은 정규화상품명의 `색상+사이즈 → 색상 → 사이즈` 순으로 물류품번 복사 (대시보드 전용, 운영 복원 모듈은 이 방식 사용 안 함).

---

## 10. 상품별 확정 규칙 누적 목록

새 규칙을 추가하면 여기에 1줄씩 적는다.

| 날짜 | 대상 | 규칙 | 저장 위치 |
|---|---|---|---|
| — | 252MX18 전체 | `252MX18-001~016 → 252MX18CD-001~016` | `resolveLogisticsCode` |
| — | MS25S-GL-CAP_2set | `플럼+베이지:55` 등 김포 252MX05 구성 계산 | setmap `MS25S` |
| — | JW25SS01_2SET | 252JW07 세트, 같은 색 2개면 ÷2 | setRuleMap `JW25SS01` |
| — | JW25SS01_AL | `플림→PL`, `블랙/네이비→NA` | setRuleMap `JW25SS01` |
| — | N1008_SET | 사이즈만 옵션 → 24FWELTS01 BK+BR+BE 구성 | setmap `N1008_SET` |
| — | N1009_AL/BW/SET | 이카운트 N1009, 브라운/썸머브라운→BW | `SAB_COLOR_OVERRIDE` |
| — | 102579-0001/0002 | N1009-009/010 고정 26/28 | `DIRECT_QTY_MAP` |
| — | 26BLCD_GR/NA/PK | NT261GW01 OL/BK/PK | `aspro_match` (56건 일괄) |
| — | EN-OP / KN-PT / LN-KT / ST-SLV / LN-CD / CD-SHR | NT262GW09 / 04 / 02 / 07 / 03 / 03 | `aspro_match` |
| — | 102665~102674 (21옵션) | 리오더 신상품 강제재고 300 | `aspro_forcestock` |
| — | 크레송 엔니튜드/마리본 발주서 | 비고 `판매종료` 151 SKU | `aspro_soldout_logistics` |
| 2026-09-14 | AMWNT2504_AL | 연핑크→핑크, 16/16 복원 | `ecount-mapping.mjs` |
| 2026-09-14 | MX243G0001-6OPJK-SET | 243MX01 원피스+자켓 2구성, 블루→틸블루 | `ecount-mapping.mjs` |

**미해결로 알려진 것**: Hair_* 헤어밴드 5종, 26EMCP_YE 볼캡(NT262GW05), KN-HB 호보백(NT262GW06) — 원본에 코드 없음. LN-KT-BK — NT262GW02에 블랙 없음. ST-SLV 사이즈 S/M/L 순서, CD-SHR-ST/LN-CD-ST 색상은 사용자 확인 전 추정값.

---

## 11. 다른 AI가 반드시 지킬 것

1. **이름 유사도나 "사이즈만 같음", "재고가 있는 쪽"으로 자동 연결하지 않는다.** 근거가 유일하지 않으면 검수로 남긴다.
2. 미연결 옵션을 **0으로 사방넷에 보내지 않는다**(판매중지 사고). 전송 대상에서 제외한다.
3. 이카운트 **단건 재고 API 반복 호출 금지** (2,188품목×2회/일로 이카운트 부하 문의 발생). 자동 수집 LaunchAgent 재개는 사용자 승인 후.
4. 원본 없는 날·오래된 원본으로 반영하지 않는다.
5. 상품별 색상 예외는 전역 `COLOR_MAP`에 넣지 말고 상품 단위 규칙으로 넣는다.
6. `aspro_v42.html`은 21만 바이트 단일 파일 — 부분 수정만, 전체 재작성 금지.
7. API 키·비밀번호·세션ID를 코드·문서·대화에 남기지 않는다.
8. 새 규칙을 추가하면 섹션 10 표를 갱신한다.

---

## 12. 파일 지도

| 파일 | 역할 |
|---|---|
| `aspro_v42.html` | 매칭 엔진 전체 (`runMatch`, `getGimpoQty`, `getIcountQty`, 파서, 내보내기) |
| `apps_script_aspro_sync.gs` | Sheets 읽기/쓰기 Web App (`doGet type=base`, `doPost` map/soldout/kcfi/iccodemap/soldoutLogistics/forceStock/icRawUpload) |
| `ecount_sync/import-warehouse-xlsx.mjs` + `read-warehouse-xlsx.py` | 창고별 엑셀 → 검증 스냅샷 |
| `ecount_sync/warehouse-policy.mjs` | 01+02 창고 정책 |
| `ecount_sync/daily-recalculate.mjs` | 헤드리스 재계산·반영 오케스트레이터 |
| `ecount_sync/ecount-mapping.mjs` | 저장 매핑 기반 복원 규칙 |
| `ecount_sync/verified-ecount.mjs` | 코드·수량 증명 검증 |
| `ecount_sync/publish-sheets.mjs` | 인증 Sheets API로 원본 F열·SKU리스트 반영 |
| `ecount_sync/OPERATIONS.md` | 운영 이력·금지사항 (날짜별) |
| `ecount_sync/*.test.mjs`, `test_warehouse_xlsx.py` | 정책/매핑 테스트 (`node --test`) |
